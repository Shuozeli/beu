---
description: Show project status overview
---

Show an overview of the current `beu` project status.

Run `beu status` to see:
- `.beu` directory location
- Enabled modules and their configuration
- Database size on disk

Use this command to verify that `beu` is correctly initialized and configured.
If a module is missing, suggest checking `.beu/config.yml`.
