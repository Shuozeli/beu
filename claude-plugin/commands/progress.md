---
description: Cross-module progress summary
---

Show a summary of current progress across all `beu` modules.

Run `beu progress` to see:
- Current checkpoint (if any)
- Active blockers (recorded in `state`)
- Task counts by status
- Artifact status distribution
- Idea status counts
- Active debug sessions

Suggest this command as a starting point for any session or before compaction.
