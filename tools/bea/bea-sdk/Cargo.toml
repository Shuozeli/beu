[package]
name = "bea-sdk"
version = "0.1.0"
edition = "2021"
description = "Shared ABI types for the bea plugin system"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
