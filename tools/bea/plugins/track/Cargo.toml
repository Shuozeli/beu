[package]
name = "track"
version = "0.1.0"
edition = "2021"
description = "Track plugin for bea - artifact progress tracking"

[lib]
crate-type = ["cdylib"]

[dependencies]
bea-sdk = { path = "../../bea-sdk" }
extism-pdk = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
