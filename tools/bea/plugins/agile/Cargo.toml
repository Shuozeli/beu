[package]
name = "agile"
version = "0.1.0"
edition = "2021"
description = "Agile plugin for bea - task and issue engine"

[lib]
crate-type = ["cdylib"]

[dependencies]
bea-sdk = { path = "../../bea-sdk" }
extism-pdk = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
